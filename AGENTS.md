# He-3 脉冲信号模拟系统：Codex 项目规则

## 权威文档

执行任务前按顺序阅读：

1. `AGENTS.md`
2. `docs/PROJECT_SPEC.md` 中与当前任务相关的章节
3. 用户指定的 `docs/phases/phase_XX_*.md`
4. `docs/STATUS.md`

发生冲突时，优先级为：用户当前明确指令 > 当前阶段文档 > `AGENTS.md` > `PROJECT_SPEC.md`。

## 固定项目边界

1. 首版模拟 He-3 热中子探测器的前置放大器输出脉冲。
2. 输入是死时间之前的真实到达率，范围 `10 ~ 1e7 cps`。
3. 首版非相关中子到达采用恒定计数率齐次泊松过程。
4. 所有真实事件必须保留并参与连续波形叠加。
5. 死时间只影响观测/触发层，不得通过删除真实事件代替堆积。
6. 首版能谱为参数化 He-3 理论能谱，预留 MCNP 结果导入接口。
7. 单脉冲采用峰值归一化双指数模型，`A_peak` 表示离散波形目标峰值。
8. 采样率支持 `100~250 MS/s`；波形时长可固定，也可按目标事件数自适应。
9. 当前只做离线软件原型，不实现 GUI、Web、DT5800 控制或实时硬件输出。
10. DT5790 数据不是核心依赖；后续示波器数据才是主要标定来源。
11. 神经网络 A 是研究对照，不得无依据替代精确泊松生成器。
12. 神经网络 C 只能学习物理波形残差，不得破坏事件计数、到达时间和能谱统计。

## 参数状态

所有参数必须标记为以下之一：

- `synthetic_demo`：只用于演示程序可运行；
- `provisional`：由有限数据或文献估计，等待人工审核；
- `validated`：有明确实验或标定依据且已人工确认。

禁止把临时参数写成真实设备参数。

## 工程规范

- Python 3.11+，`pyproject.toml`，`src/` 布局。
- 公共 API 必须有类型注解和 docstring。
- 内部物理量优先使用 SI 单位，字段名显式带单位。
- 随机过程使用 `numpy.random.Generator`，显式保存 seed。
- 配置、代码版本、参数状态、单位和 seed 写入输出元数据。
- 物理核心不得依赖 PyTorch。
- 多进程必须兼容 Windows `spawn`。
- 优化实现必须有简单参考实现用于回归测试。
- 不允许硬编码统计结果、静默吞掉异常或为通过测试篡改物理逻辑。
- Markdown 文档统一保存为 UTF-8；展示公式使用独占一行的 `$$` 分隔符，不使用会被当前渲染器显示为普通文本的 LaTeX 方括号式分隔符。
- 修改代码后运行对应格式、类型和测试命令。

## 阶段门禁

- 一次只执行一个阶段。
- 不得提前实现后续阶段。
- 当前阶段未通过验收时，不得宣称完成。
- 未经用户确认，不得进入下一阶段。

## 每次完成任务后必须报告

1. 新增/修改文件；
2. 关键设计决定；
3. 实际运行的命令；
4. 测试和检查结果；
5. 尚未解决的问题与风险；
6. 当前阶段是否通过验收；
7. `docs/STATUS.md` 的更新内容。
